[package]
name = "c2proof"
version = "0.1.0"
edition = "2021"
description = "C to Rust migration with verification report. Verifier-first."
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[dev-dependencies]
tempfile = "3"
